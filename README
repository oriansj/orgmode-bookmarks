A simple set of tools for converting between org-mode files and sqlite bookmark files
To simplify the task of keeping ones bookmarks in version control
ALL code in this REPO is under the GPLv3 or Later.
